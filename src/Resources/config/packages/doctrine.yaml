doctrine:
    orm:
        entity_managers:
            default:
                mappings:
                    UserAction:
                        is_bundle: false
                        type: attribute
                        dir: '%kernel.project_dir%/src/Entity'
                        prefix: 'ACSEO\SyliusAdminTrackerPlugin\Entity'
                        alias: UserAction
